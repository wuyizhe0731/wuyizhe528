var lis =["1","absd","435"];
console.log(lis)
var needR ="";
var res = ''
for (var i = 0 ; i < lis.length ; i++){
	needR = lis[i]
	//console.log(needR[0])
	// for (var ii = 0; ii < needR.length ; ii++){
	// 	res += needR[needR.length - 1 - ii]
	// 	console.log(res)
	console.log(needR)
	}

//}
